基于go语言的一个简单的接口+静态资源服务器


```
应用场景1:
由于在浏览器中直接双击文件夹中的index.html在浏览器中的路径是file://***, 
如果访问某些资源会出现CORS的问题, 因此需要将项目放置到静态资源服务器下, 如http-server, 
在项目中启动http-server, 使用localhost::8088来访问项目的public, 就避免了CORS。

应用场景2：
前端框架如vue打包后的项目，部署运行。
```